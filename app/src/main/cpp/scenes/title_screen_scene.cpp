#include "title_screen_scene.hpp"

TitleScreenScene::TitleScreenScene() = default;

TitleScreenScene::~TitleScreenScene() = default;

void TitleScreenScene::DoFrame() {
    // For now, this scene doesn't do anything.
}
